あああああ
20221113 Test

●タイルとマスの違いについて(提案)
	タイル…文字から生成するときに使うクラス
	マス…生成されたステージ上の1つのグリッドのこと

定数
[SerializeField]
変数
プロパティ

StageGeneratorコンポーネントは完全に独立させてある
使用する際にはヒエラルキーにParent(生成したオブジェクトの親)を作り参照させる。
	文字とそれに対応するプレハブを設定する必要がある。
ステージを生成する場合は、StageGeneratorのGenerate()メソッドを呼ぶ
	引数は文字の二次元配列
ステージ上の障害物を生成する場合は、StageGeneratorのGenerateObst()メソッドを呼ぶ
	引数は文字の二次元配列